# properties file for holding application configuration

#application server configuration
spring.application.name = message-queue

# the type of source the message queue accept messages from  
accepted.message.source = newspaper
